Create an virtual environment and add requirements.txt file to your virtual environment
Command to upload requirements.txt : pip freeze -r requirements.txt
This command will install all the packages.
